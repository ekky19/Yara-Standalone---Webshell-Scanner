<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>🛡️ YARA Detection Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f9;
            margin: 0;
            padding: 20px;
        }

        .header {
            text-align: center;
            padding: 20px 0;
        }

        .header h1 {
            font-size: 28px;
            color: #22313f;
        }

        .section-title {
            text-align: center;
            font-size: 22px;
            color: #22313f;
            font-weight: bold;
            margin: 20px 0 10px;
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .card {
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 12px;
            padding: 20px;
            width: 360px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        .card h3 {
            margin-top: 0;
            color: #2c3e50;
            font-size: 20px;
        }

        .card p {
            margin: 8px 0;
            font-size: 14px;
        }

        .footer {
            text-align: center;
            font-size: 13px;
            color: #eee;
            padding: 15px;
            margin-top: 40px;
            background-color: #2c3e50;
        }

        .footer a {
            color: #a9c6f2;
            text-decoration: none;
        }

        .hash-link {
            word-break: break-all;
            color: #0645AD;
        }
    </style>
</head>
<body>

    <div class="header">
        <h1>🛡️ YARA Detection Report</h1>
    </div>

    <div class="section-title">Scanned File: {{ grouped_records | first }}</div>

    <div class="card-container">
        {% set matches = grouped_records[grouped_records | first] %}
        {% for m in matches %}
        <div class="card">
            <h3>{{ m.rule }}</h3>
            <p><strong>YARA File:</strong> {{ m.yara_file }}</p>

            {% for k, v in m.meta.items() %}
                {% set v_str = (v|string)|trim %}
                {% if v_str %}
                    <p><strong>{{ k|capitalize }}:</strong> {{ v_str }}</p>
                {% endif %}
            {% endfor %}

            <p><strong>SHA-256:</strong><br>
                <a class="hash-link" href="https://ekky19.github.io/osint/?ip={{ m.sha256 }}" target="_blank">{{ m.sha256 }}</a>
            </p>
        </div>
        {% endfor %}
    </div>

    <div class="footer">
        Report generated by WebShell Analyzer<br>
        Developed by <a href="https://www.linkedin.com/in/ekremozdemir19/" target="_blank">Ekrem O.</a>
    </div>

</body>
</html>
